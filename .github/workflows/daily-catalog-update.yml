name: Daily catalog update preview

on:
  schedule:
    - cron: "17 10 * * *"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  preview-update:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "22"

      - name: Run sample-feed update preview
        run: node scripts/update-catalog.js --feed fixtures/daily-update-feed.json --report artifacts/daily-update-report.json --snapshot artifacts/catalog-update-preview.json

      - name: Upload update report and preview
        uses: actions/upload-artifact@v4
        with:
          name: daily-catalog-update-preview
          path: |
            artifacts/daily-update-report.json
            artifacts/catalog-update-preview.json
          if-no-files-found: error
          retention-days: 14
